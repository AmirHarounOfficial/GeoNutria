
  # Replicate Existing App

  This is a code bundle for Replicate Existing App. The original project is available at https://www.figma.com/design/vf66AZqPbYoB8L46wIvOsN/Replicate-Existing-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  